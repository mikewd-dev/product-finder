[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"  

[functions]
  directory = "netlify/functions"

[dev]
  framework = "vite"
  command = "npm run dev"
  targetPort = 5000
 
